# The build context is the repo root, but the Containerfile only COPYs
# build/root/. Without this, every build sends podman the whole repo as
# context — node_modules and the other build/ artefacts (build/card,
# build/qemu, disk images) run into multiple gigabytes, on every build.
*
!build/root
